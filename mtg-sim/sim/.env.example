# Optional overrides only. Defaults live in the repo-root .env (see /.env.example).
#
# Copy to .env here ONLY when you need machine-specific secrets or paths that
# should not live in the shared root .env, for example:
#   FORGE_JAR=/absolute/path/to/forge-gui-desktop.jar
#
# start.sh and sim/main.py load repo-root .env first, then this file.
